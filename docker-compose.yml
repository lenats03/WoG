version: "3.7"
services:
  web:
    build: .
    ports:
      - "8777:5001"
    image: "lenats/wog:${BUILD_ID}"
    container_name: $containername
  dbb:
     image: mysql:5.7
     container_name: mysql_db
     restart: always
     environment:
        - MYSQL_ROOT_PASSWORD="secret"